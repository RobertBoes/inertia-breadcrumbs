<?php

namespace Laravel\Octane\Events;

/**
 * Stub standing in for laravel/octane's event so the service provider's
 * class_exists() guard passes and the listener can be exercised without
 * depending on laravel/octane.
 */
class RequestReceived {}
